import express from 'express'

const DashboardController = express.Router()

DashboardController.get('getValues', async (req, res) => {
    /// Obtenemos todos los datos para poder mostrarlos en el dashboard
})